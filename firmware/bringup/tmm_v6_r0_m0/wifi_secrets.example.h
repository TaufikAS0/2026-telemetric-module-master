#pragma once

// Copy this file to wifi_secrets.h and replace both placeholders locally.
// wifi_secrets.h is ignored by Git and must never be committed.
constexpr char TMM_WIFI_SSID[] = "YOUR_WIFI_SSID";
constexpr char TMM_WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
